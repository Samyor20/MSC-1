#include<stdio.h>
#include<stdlib.h>
#include<sys.stat.h>
#include<errno.h>

void main( int argc , char *argv[]) 
{
if(argc != 2) {
fprintf(stderr, "usage: </filename>",argv[0])
return 1;
}

const char *file_name = argv[0];
struct stat file_st;

if(stat(filename,&file_st == 0)) 
{
printf("The File %s exists in the current directory ");
} else {
  if(errno == ENONENT)
printf("The file %s does not exists in this directory");
} else {
peror("Error geting the file status. ");
}
return 0;
}



}

